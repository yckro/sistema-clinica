import { COLORS, FONT_SIZES } from "../../constants/theme.js";

export const styles = {

    container: {
        backgroundColor: COLORS.white,
        flex: 1,
        padding: 12,
    },
    text: {
        fontSize: FONT_SIZES.md,
        Color: COLORS.gray1,
        marginBottom: 15,
        textAlign: "center"
    }
}