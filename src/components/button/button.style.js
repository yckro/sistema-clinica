import {COLORS, FONT_SIZES} from "../../constants/theme";

export const styles = {
    btn: {
        backgroundColor: COLORS.blue,
        padding: 12,
        borderRadius: 6,
        width: "100%",
        
        
    },
    text: {
        color: COLORS.white,
        fontSize: FONT_SIZES.md,
        textAlign: "center",
    }
}